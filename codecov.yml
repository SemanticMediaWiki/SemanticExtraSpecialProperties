fixes:
  - "/var/www/html/extensions/SemanticExtraSpecialProperties/::"

coverage:
  status:
    project:
      default:
        informational: true
        removed_code_behavior: adjust_base
    patch:
      default:
        informational: true
